<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.user">
	<insert id="insertUser" parameterType="user">
		insert into user
		(user_id,password,name,phone,email,birth)
		values(#{userId},#{password},#{name},#{phoneNum},#{email},#{birth})
	</insert>

	<select id="selectUser" parameterType="String" resultType="user">
		select * from user where user_id=#{userId}
	</select>

	<update id="updateSingleField" parameterType="map">
		UPDATE user
		SET ${column} = #{value}
		WHERE user_id = #{userId}
	</update>
	
	<update id="withdrawUser" parameterType="String">
		update user
		set deleted = #{deleted}, withdrawal_reason = #{withdrawalReason},withdrawal_detail=#{withdrawalDetail}
		where user_id = #{userId}
		
	</update>

</mapper>