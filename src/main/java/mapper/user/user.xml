<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="mapper.user">
	<insert id="insertUser" parameterType="user">
		insert into user (user_id,password,name,phone,email,birth) 
			values(#{userId},#{password},#{name},#{phoneNum},#{email},#{birth})
	</insert>
	
	<select id="selectUser" parameterType="String" resultType="user">
		select * from user where user_id=#{userId}
	</select>
	

  <select id="selectUserPoint" parameterType="String" resultType="int">
    select point from user where user_id = #{userId}
  </select>


  <select id="selectUserAddressList" parameterType="String" resultType="user">
    select address1, address1_detail, address2, address2_detail, address3, address3_detail
    from user where user_id = #{userId}
  </select>
  

<select id="loginUser" parameterType="map" resultType="user">
  SELECT *
  FROM user
  WHERE user_id = #{userId}
    AND password = #{password}
</select>
  
	
</mapper>