<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="mapper.category">

    <insert id="insertCategory" parameterType="dto.admin.Category">
        INSERT INTO Category (category_id, category_name)
        VALUES (#{categoryId}, #{categoryName})
    </insert>

    <select id="selectCategoryList" resultType="dto.admin.Category">
        SELECT category_id AS categoryId, category_name AS categoryName
        FROM Category
    </select>

    <insert id="insertSubCategory" parameterType="dto.admin.SubCategory">
        INSERT INTO SubCategory (sub_category_id, sub_category_name, category_id)
        VALUES (#{subCategoryId}, #{subCategoryName}, #{categoryId})
    </insert>

    <select id="selectSubCategoryList" resultType="dto.admin.SubCategory">
        SELECT sub_category_id AS subCategoryId, sub_category_name AS subCategoryName, category_id
        FROM SubCategory
    </select>

</mapper>

	